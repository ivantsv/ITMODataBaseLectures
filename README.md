# 📚 Полный конспект по Базам Данных (ИТМО)

## 1. Основы и терминология
*   **5 информационных процессов:** Сбор, Обработка, Хранение, Передача, Представление.
*   **Данные:** Поддающиеся многократной интерпретации сведения в формализованном виде, пригодном для обработки и передачи.
*   **Целостность:** Инфа не дублируется, легко обновляется.
*   **Гибкость:** Легко связывается.
*   **Зависимость:** Много отношений $\rightarrow$ Целостность ↑, Гибкость ↑, Производительность ↓.
*   **Требования к БД:** Структура, Целостность, Многопользовательский доступ, Безопасность, Язык запросов.

## 2. Моделирование данных
*   **Модель данных:** Формальное описание того, как будем хранить данные.
*   **Уровни моделирования:** Концептуальный (бизнес-сущности) $\rightarrow$ Логический $\rightarrow$ Физический.
*   **СУБД (Система управления БД):** Данные не привязаны к железу $\rightarrow$ классная абстракция.
*   **Виды моделей данных:**
    1. Реляционная
    2. Документоориентированная
    3. Графовая
    4. KV (Key-Value)
    5. Временные ряды
    6. Векторная
    7. Колоночная
    8. Хранилище данных
*   **ERD (Entity Relation Diagram):** Диаграмма «Сущность-связь».
    *   Связи: `one-to-one`, `one-to-many`, `many-to-many`.
    *   Сущность: сильная и слабая.

## 3. Реляционная модель и SQL
*   **Отношение** = Таблица.
*   **Кортеж** = Строка в отношении.
*   **Атрибут** = Столбец в отношении.
*   **Виды атрибутов:** Простой, Составной, Однозначный, Многозначный, Производный.
*   **Домен атрибута:** Допустимое значение атрибута.
*   **Кардинальность отношения:** Кол-во кортежей (строк).
*   **Арность (степень) отношения:** Кол-во атрибутов (столбцов).
*   **Реляционные ключи:**
    *   **Супер-ключ:** Набор атрибутов, которые уникально идентифицируют каждый кортеж.
    *   **Потенциальный ключ:** Минимальный супер-ключ.
    *   **Первичный ключ (PK):** Выбранный потенциальный.
    *   **Внешний ключ (FK):** Ссылается на PK из другой таблицы.
    *   **Альтернативный ключ:** Не выбранный потенциальный.
    *   **Подвиды:** Простой, составной, естественный, суррогатный.
*   **Языки:**
    *   **DML (Data Manipulation Lang):** `SELECT`, `INSERT`.
    *   **DDL (Data Definition Lang):** `CREATE TABLE`, `ALTER TABLE`, `DROP`, `TRUNCATE`.

## 4. Реляционная алгебра
*   **Типы операций:** Теоретико-множественные и Специальные реляционные.
*   **Операции:**
    *   Выборка ($\sigma$)
    *   Проекция ($\pi$)
    *   Объединение, Пересечение, Разность.
    *   Декартово произведение ($\times$).
    *   **Деление ($\div$):** Обратное декартову произведению.
    *   **Соединение ($\Join$):** Тета-соединение, соединение по эквивалентности, естественное соединение.

## 5. Нормализация
*   **Цель:** Устранение избыточности и обеспечение целостности.
*   **Аномалии:**
    *   *Добавления:* Нельзя добавить данные об одном объекте, не добавив о другом.
    *   *Удаления:* При удалении одних данных невольно теряем другие.
    *   *Обновления:* Надо менять в нескольких местах одновременно.
*   **Функциональная зависимость:** Из $A \to B$ (однозначно).
*   **Нормальные формы:**
    *   **1NF:** Атомарность, без вложенных таблиц, кортеж уникален.
    *   **2NF:** 1NF + зависимость от *составного* ключа (все атрибуты зависят от всех частей ключа). *Плохо: (student_id, course_id) -> course_name.*
    *   **3NF:** 2NF + значение атрибута зависит только от ключа и не зависит от неключевого атрибута. *Плохо: (id) -> dept_id -> dept_name.*
    *   **НФБК (Бойса-Кодда):** Если атрибут определяет другие — он должен быть ключом.
    *   **4NF:** Убираем независимые многозначные зависимости. *(Actor, Film, Language $\to$ Actor, Language).*
    *   **5NF:** Самая жесткая. Разбиваем отношение на много частей, убираем странные зависимости, но при этом можем соединить обратно.

## 6. Физическое устройство и LSM
*   **LSM-дерево (Работа):**
    *   **Запись:** WAL $\rightarrow$ MemTable. Если MemTable full? $\rightarrow$ **Flush SST**. Фоновый merge/compact.
    *   **Чтение:** Фильтр Блума $\rightarrow$ MemTable $\rightarrow$ SST (может быть еще фильтр Блума на SST).
    *   **Levels:** SSTables организованы в уровни (L0, L1...). Чем выше уровень, тем больше данных и тем они старее.
*   **Этапы выполнения запроса:** Лексический $\rightarrow$ Семантический $\rightarrow$ Планирование (+ `EXPLAIN`). *Пояснить за составляемый граф.*
*   **Хранение статистики:** `pg_statistics` и пр.
*   **Общие / Частные планы:** Кэширование / Буферизация (батчинг для балансировки I/O).
*   **Shared Buffers:** Просто копия страниц/индексов в RAM. Оперативка внутри СУБД (настоящий RAM).
    *   Кэш (ускорение чтения).
    *   Буфер (накопление записей перед сбросом на диск).

## 7. Индексы и Транзакции (ACID)
*   **Индексы:** СД для ускорения выборки. Чтение ↑, Запись ↓.
    *   Типы: `Hash`, `B-tree`, `GiST`, `R-tree`, `KNN`, `GIN`.
*   **ACID:**
    *   **A (Atomicity):** Транзакция выполнена полностью или не выполнена.
    *   **C (Consistency):** После завершения — БД в согласованном состоянии.
    *   **I (Isolation):** Параллельные транзакции как последовательные.
    *   **D (Durability):** Надежность после commit.
*   **Аномалии транзакций:** Потерянное обновление, Грязное чтение (uncommitted read), Фантомное чтение.
*   **Уровни изоляции:** `Read Uncommitted`, `Read Committed`, `Repeatable Read`, `Serializable`.

## 8. Работа соединений и методы доступа
*   **Соединения:**
    1. Хешированием (+ параллельное соединение хешированием — шарды).
    2. Слиянием (ключи отсортированы / сортируем через Quicksort в PG).
    3. Вложенным циклом (внешняя — маленькая, у внутренней — индекс).
*   **Селективность:** Высокая (мало строк), Низкая (много строк).
*   **Методы доступа:**
    1. Посл. сканирование (`Seq Scan`) $\rightarrow$ Низкая селективность.
    2. Индексы $\rightarrow$ Высокая селективность.
    3. Гибрид (Индекс $\rightarrow$ Bitmap $\rightarrow$ Посл.) $\rightarrow$ Средняя селективность.
*   **Gather + Gather Merge** (Параллелизм). Оптимизатор считает стоимость.

## 9. Безопасность и Программирование
*   **Безопасная система:** Защищена от несанкционированного доступа.
*   **CIA Triad:** Confidentiality (конфид.), Integrity (целостность), Availability (доступность).
*   **Процессы:** Идентификация (кто это?) $\rightarrow$ Аутентификация (докажи! вход) $\rightarrow$ Авторизация (что можно? права).
*   **Модели доступа:**
    1. **DAC (Дискреционная):** Владелец сам решает кому какие права. Использует ACL.
    2. **MAC (Мандатная):** Центральная политика, владелец ничего не решает. Сверхбезопасные системы. Нельзя передавать права.
*   **PostgreSQL:** Привилегии (на БД, схеме, таблице, колонках), Роли (users, группы, автоматические права), Наследование привилегий.
*   **Функции / Процедуры / Триггеры:**
    *   Функции: что-то возвр., `SELECT`. Волатильность: `IMMUTABLE`, `STABLE`, `VOLATILE`.
    *   Процедуры: управляют транзакциями.
    *   Триггеры: автоматическая реакция на `INSERT`/`UPDATE`/`DELETE`.

## 10. Распределенные БД
*   **Горизонтальное масштабирование:**
    *   **Репликация:** (Копии). Архитектуры: Master-Slave, Master-Master, Peer-to-Peer. Синхронная / Асинхронная.
    *   **Шардинг:** (Разделение). Стратегии: По диапазону, По хешу, По справочнику, Геошардинг.
    *   **Партиционирование $\neq$ Шардирование!**
*   **Проблемы шардирования:** Join'ы, Агрегаты, Решардинг, Нужен координатор.
*   **Типы распред. СУБД:** Однородные / Неоднородные.
*   **2PC (Two-phase commit):** Безопасность в распределенке.
*   **CAP (Брюера):** CP (консистентность при разрыве) vs AP (доступность при разрыве).
*   **PACELC:** P-A-C (при аварии) + E-L-C (Else — в штатном режиме: Latency vs Consistency).
*   **BASE:**
    *   **BA:** Basically Available (всегда отвечает).
    *   **S:** Soft state (состояние может быть не мгновенно согласовано).
    *   **E:** Eventual consistency (со временем станет консистентным).
 

*   **Уточнение по фантомам:**
    *   **Non-repeatable read (Неповторяющееся чтение):** это когда ты прочитал **строку**, а кто-то её **изменил или удалил**.
    *   **Phantom read (Фантомное чтение):** это когда ты прочитал **набор строк** (например, всех студентов с оценкой "5"), а кто-то **вставил (INSERT)** нового студента. В итоге при повторном запросе у тебя в списке появился «призрак» (новая строка).
*   **Про уровни:** Молодец. Маленький «бонус»: в PostgreSQL уровень `Repeatable Read` **уже** защищает от фантомов (хотя по стандарту SQL не обязан). `Serializable` в Postgres — это еще более мощная штука (SSI), которая следит за зависимостями между транзакциями.

*   **Термин:** Такая страница в памяти называется **Dirty Page** («Грязная страница»). Это страница, данные в которой в RAM новее, чем на диске.
*   **Сбой:** 
    *   **Секрет выживания:** СУБД сначала пишет изменение в лог на диск (WAL), и только потом меняет страницу в RAM (Shared Buffers). Лог — это просто длинный список «что мы сделали». 
    *   **Восстановление:** Когда свет включат, СУБД прочитает WAL и «проиграет» все операции заново (**Redo**), восстановив те изменения, которые не успели долететь до основных файлов данных.


* **Cost - основа выбора метода планировщиком. Поиск, обход и прочее**

* **Цель 3NF - устранить транзитивные зависимости**
* **IMMUTABLE для на самом деле таковой не являющейся - ломает БД из-за кэширования**
* **Serializable - про зависимость транзакций. Repeatble read - снимок данных**
* **В 3НФ может быть зависимость из не ключа в ключ ((Преподаватель - Студент) - Предмет), НФБК - такое запрещает**
* WAL — общая концепция для всех надежных БД. Нужно для восстановления после сбоев (Durability).
* MemTable (в LSM) и Shared Buffers (в PG) — оба ускоряют работу за счет RAM, но MemTable собирает новые данные, а Buffers кэшируют старые.
* SSTable — это специфика NoSQL и LSM-систем. В классических реляционках (Postgres) вместо них — файлы данных, разбитые на страницы.

Это **«Мега-конспект 2.0»**. Я взял твою исходную структуру и «заполнил пробелы» ответами на все те глубокие и каверзные вопросы, которые мы обсуждали. Это идеальная шпаргалка, где собраны и базовые определения, и «высший пилотаж» для отличной оценки.

---

## I. Основы, Логика и Данные
*   **Данные vs Информация:** Данные — это сырые формализованные факты (`38`). Информация — данные в контексте (`Температура 38°`). 
*   **DIKW:** Data → Information → Knowledge (опыт) → Wisdom (действие).
*   **Реляционное произведение ($\times$):** Комбинирует **каждую** строку А с **каждой** строкой Б. 
    *   *Результат:* Столбцы = $A + B$, Строки = $A \times B$.
*   **Реляционное деление ($\div$):** Обратно произведению. Используется для запросов со словом **«ВСЕ»** (найти студентов, прошедших *все* курсы). 
    *   *В SQL:* Реализуется через двойной `NOT EXISTS`.
*   **ACID и Реляционность:** **Не связаны напрямую.** ACID — это про надежность транзакций, Реляционность — про структуру таблиц. 
    *   *Пример:* NoSQL (Neo4j) может быть ACID, а Реляционная БД (MySQL MyISAM) — нет.

## II. Физическое устройство и Хранение (LSM vs B-Tree)
*   **LSM-Tree (Архитектурный паттерн):** Оптимизировано для записи (**Append-only**). 
    *   **Цепочка:** WAL (лог) → MemTable (RAM дерево) → Flush → SSTable (диск).
    *   **Зачем:** Превращает медленную случайную запись в быструю последовательную.
*   **SSTable (Sorted String Table):** Неизменяемый файл на диске. Состоит из данных, **разреженного индекса** (Sparse Index) и **фильтра Блума** (чтобы не читать диск, если ключа точно нет).
*   **B-Tree (Классика):** Оптимизировано для чтений и обновлений на месте (**In-place update**). Использует **Shared Buffers**.
*   **Shared Buffers vs MemTable:** 
    *   `Shared Buffers` (Postgres) — это кэш старых страниц, которые мы перезаписываем. 
    *   `MemTable` (LSM) — это буфер для сборки новых данных, которые станут новым файлом.
*   **Cache vs Buffer:** Кэш ускоряет **повторное** чтение. Буфер сглаживает разницу скоростей (Batching) и ускоряет **запись**.

## III. Индексы и Оптимизация
*   **B-Tree:** Стандарт де-факто (хотя в SQL не прописан). Для `=`, `>`, `<`, `BETWEEN`.
*   **R-Tree:** Для геометрии. Использует **MBR** (прямоугольники-матрешки). В Postgres — часть **GiST**.
*   **KNN (Ближайшие соседи):** Похож на B-tree иерархией. В 1D B-tree — это идеальный KNN. В 2D+ используется GiST и оператор расстояния `<->` (алгоритм Best-First Search).
*   **Колоночные БД (OLAP):** Хранят данные по столбцам. 
    *   *Плюсы:* Сжатие в 10-100 раз, чтение только нужных колонок, векторная обработка. 
    *   *Минус:* Нельзя быстро вставить одну строку.
*   **Статистика:** Гистограммы и MCV (частые значения). Если `rows` в плане сильно врет — `ANALYZE` протух.

## IV. Выполнение запросов (Explain)
*   **Подготовленный запрос (Prepared Statement):** Шаблон (`$1`). 
    *   *Зачем:* 1. Безопасность (нет SQL Injection). 2. Скорость (план строится один раз).
*   **Общий vs Частный план:** 
    *   `Частный`: под конкретное значение (точно, но дорого строить). 
    *   `Общий`: шаблон (быстро, но может быть неэффективен). 
    *   *Postgres:* Первые 5 раз строит частные, на 6-й сравнивает их с общим и выбирает лучший путь.
*   **Профилирование:** `EXPLAIN ANALYZE`. Реальное выполнение запроса с замером времени и `BUFFERS` (Hit/Read).

## V. Транзакции и Конкурентность (MVCC)
*   **MVCC:** «Читатели не ждут писателей». Реализуется через версии строк (**xmin/xmax**). Старые версии чистит **VACUUM**.
*   **Lost Update (Потерянное обновление):** Когда Т2 затирает изменения Т1.
    *   *Как фиксит Repeatable Read:* В Postgres он выдает **ошибку сериализации**, если строка была изменена кем-то другим после снятия снимка. Программист должен сделать **Retry**.
*   **Serializable:** Исключает **Write Skew** (пример с врачами на дежурстве). В Postgres работает через граф зависимостей (SSI).
*   **Согласованность (Consistency):** 
    *   В **ACID** — это правила (Constraints, FK). 
    *   В **CAP** — это одинаковые данные на всех узлах.

## VI. Проектирование (Нормализация)
*   **3НФ:** Нет транзитивных зависимостей. Все неключевое зависит только от ключа.
*   **НФБК (Бойса-Кодда):** Строже 3НФ. Устраняет аномалии в **перекрывающихся составных ключах**. 
    *   *Правило:* Любой детерминант обязан быть суперключом. 
    *   *Пример:* Студент-Преподаватель-Предмет.
*   **4НФ:** Устраняет **нетривиальные многозначные зависимости** (когда в одной таблице два независимых списка, например Актер-Фильмы и Актер-Языки).

## VII. Распределенные системы и Безопасность
*   **2PC (Двухфазный коммит):** 1. Prepare (Все готовы?). 2. Commit (Все пишем). Если один "No" — Rollback всем.
*   **PACELC:** Расширение CAP. 
    *   Если авария (P) → выбор между **A** и **C**. 
    *   Если всё ок (E) → выбор между **L** (задержка) и **C** (согласованность).
*   **BASE:** Противоположность ACID для NoSQL (Eventual Consistency).
*   **Модели доступа:** 
    *   **DAC:** Владелец сам рулит правами (GRANT). 
    *   **MAC:** Система рулит по меткам (Секретно/Допуск). Пользователь не может передать право другу.

* **Статистика - набор данных, который собирается из значений в строках и столбцах. Используется планировщиком для выбора наиболее эффективного плана исполнения**

Этот финальный блок дополняет твой конспект глубокими техническими деталями, которые мы разобрали в ходе обсуждения. Это те самые «нюансы на пятерку», которые показывают полное понимание архитектуры СУБД.

## 1. Механизмы выполнения запросов (Physical Joins)
В дополнение к общему списку соединений, важно понимать внутреннюю логику **Hash Join**:

*   **Фаза Build (Построение):** СУБД берет меньшую таблицу и строит в RAM хеш-таблицу по ключу соединения.
*   **Фаза Probe (Зондирование):** СУБД сканирует вторую таблицу, хеширует её ключи и мгновенно ищет совпадения в хеш-таблице.
*   **Hybrid Hash Join:** Если хеш-таблица не влезает в память (`work_mem`), СУБД разбивает данные на **пачки (batches)**, сбрасывает их на диск и соединяет по очереди.
*   **Ограничение:** Работает только для оператора равенства (`=`).

## 2. Безопасность: Роли и Группы в PostgreSQL
В Postgres нет разделения на «пользователей» и «группы» на уровне ядра — есть только **Роли (Roles)**.

*   **Роль как Пользователь:** Роль с атрибутом `LOGIN`. Может подключаться.
*   **Роль как Группа:** Роль с атрибутом `NOLOGIN`. Служит контейнером для прав.
*   **Механизм:** 
    *   `GRANT group_role TO user_role` — включение в группу.
    *   **Наследование (`INHERIT`):** Пользователь автоматически получает права всех своих групп.
    *   **Глобальность:** Роли создаются на уровне всего кластера СУБД, а не внутри конкретной базы.

## 3. Распределенные архитектуры: Multi-Master vs P2P
*   **Multi-Master:** Группа выделенных серверов-мастеров. Каждый пишет, данные синхронизируются. Сложно масштабировать более чем на 15–20 узлов из-за конфликтов.
*   **Peer-to-Peer (Одноранговая):** Все узлы равны. 
    *   **Gossip Protocol:** Узлы обмениваются состоянием через «сарафанное радио» (слухи).
    *   **Consistent Hashing:** Автоматическое распределение данных по «кольцу» узлов (шардинг вшит в архитектуру).
    *   **Пример:** Cassandra (AP по теореме CAP).

## 4. Транзакции: Глубокий MVCC и Аномалии
*   **Внутренности MVCC:** Каждая строка имеет скрытые колонки `xmin` (кто создал) и `xmax` (кто удалил/обновил). 
    *   `UPDATE` = `DELETE` (пометка `xmax`) + `INSERT` (новая строка с новым `xmin`).
    *   **Bloat (Мусор):** Накопление старых версий строк. Очищается через **VACUUM**.
*   **Lost Update в Repeatable Read:**
    *   Postgres фиксит это через **ошибку сериализации**. Если строка была изменена другой транзакцией после того, как текущая сделала свой снимок (Snapshot), попытка `UPDATE` вызовет `ERROR: could not serialize access`.
*   **Write Skew в Serializable:**
    *   Единственная аномалия, которую ловит только `Serializable`. Пример: два дежурных врача одновременно пытаются уйти со смены, думая, что второй остается. Решается через отслеживание зависимостей (SSI).

## 5. Продвинутая индексация: R-Tree и KNN
*   **R-Tree (для геометрии):**
    *   Использует **MBR (Minimum Bounding Rectangle)** — охватывающие прямоугольники.
    *   Организовано как матрешка: корень — вся карта, узлы — районы, листья — объекты.
    *   Допускает **перекрытие (overlapping)** прямоугольников, из-за чего поиск может заходить в несколько веток.
*   **KNN (K-Nearest Neighbors):**
    *   Поиск ближайших объектов через оператор расстояния `<->`. 
    *   Использует индекс (GiST) и алгоритм **Best-First Search** (приоритетная очередь обхода узлов, которые ближе всего к искомой точке).

## 6. Оптимизация и Статистика
*   **Pipeline запроса:** Парсер → Анализатор → Переписчик → Оптимизатор (CBO) → Исполнитель.
*   **Cost Units:** Стоимость в Postgres измеряется не в секундах, а в абстрактных единицах (где 1.0 — это `seq_page_cost`, чтение одной страницы с диска).
*   **Prepared Statements:**
    *   **Производительность:** План строится один раз, экономится CPU.
    *   **Безопасность:** Защита от **SQL-инъекций**, так как параметры передаются отдельно от логики запроса.
*   **Generic vs Custom Plan:** Postgres 5 раз строит частный план, на 6-й пробует общий. Если общий не сильно дороже — использует его всегда.

## 7. Модели данных: OLAP и Колонки
*   **Почему колонки лучше для аналитики (OLAP):**
    1.  **I/O Efficiency:** Читаем только нужные столбцы (не читаем лишний мусор из строк).
    2.  **Compression:** Одинаковые типы данных в колонке сжимаются в 10-100 раз (RLE, Delta-encoding).
    3.  **Vectorization:** Процессор обрабатывает данные пачками (SIMD-инструкции) вместо прохода по одной строке.

| Теория | SQL                |
| ------ | ------------------ |
| ∪      | UNION              |
| ∩      | INTERSECT / JOIN   |
| \      | EXCEPT / LEFT JOIN |
| ×      | CROSS JOIN         |
| ∈      | IN / EXISTS        |

SQL сначала формирует множество строк (FROM + JOIN),
потом фильтрует (WHERE),
затем группирует (GROUP BY),
фильтрует группы (HAVING),
вычисляет SELECT,
убирает дубликаты (DISTINCT),
сортирует (ORDER BY)
и только в конце обрезает (LIMIT).

**Определение:** Атрибут $B$ функционально зависит от $A$ ($A \to B$), если в любой момент времени для любого значения $A$ существует ровно одно связанное с ним значение $B$.
**Виды (для нормализации):**
*   **Тривиальная:** Когда $B$ является подмножеством $A$ (например, `(ID, Имя) -> Имя`).
*   **Полная:** Когда $B$ зависит от всего составного ключа, а не от его части (важно для 2НФ).
*   **Транзитивная:** Когда $A \to B$ и $B \to C$, следовательно $A \to C$ (важно для 3НФ).
*   **Многозначная:** Когда одному значению $A$ соответствует набор независимых значений $B$ (важно для 4НФ).
---
